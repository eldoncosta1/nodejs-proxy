{
  "name": "nodejs-proxy",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "NODE_ENV=dev ts-node-dev -r tsconfig-paths/register --inspect --respawn --transpile-only --exit-child --ignore-watch node_modules index.ts",
    "proxy": "NODE_ENV=dev ts-node-dev -r tsconfig-paths/register --inspect --respawn --transpile-only --exit-child --ignore-watch node_modules ./server-proxy.ts"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "http-proxy-middleware": "^2.0.6",
    "reflect-metadata": "^0.1.13",
    "ts-node-dev": "^2.0.0",
    "tsconfig-paths": "^4.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "typescript": "^4.7.4"
  }
}